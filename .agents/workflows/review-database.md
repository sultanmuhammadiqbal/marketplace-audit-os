# Review Database Workflow

1. Check if the database migration introduces a table. If yes, verify RLS is enabled.
2. Check if the policies correctly filter by `organization_id`.
3. Ensure the types are generated.
