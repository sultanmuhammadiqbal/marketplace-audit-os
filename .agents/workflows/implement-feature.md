# Implement Feature Workflow

1. Discuss and refine the requirement with the user.
2. Outline the database changes needed (if any).
3. Outline the API and Server Action changes.
4. Build the UI components (shadcn/ui + Tailwind).
5. Integrate the UI with the backend (Server Actions / Data fetching).
6. Write or update tests.
7. Perform manual verification.
